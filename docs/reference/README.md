# Reference Docs

> **Applies to:** RM520N-GL (SDX65) · verified 2026-08
> **RG501Q-EU (SDX55):** unverified — see [`platform-matrix.md`](./platform-matrix.md)

Detailed operational notes extracted from `CLAUDE.md` to keep the always-loaded project instructions lean. Each file is self-contained — read it only when working on that subsystem.

| Doc | Read when you are working on... |
|-----|---------------------------------|
| [at-command-transport.md](at-command-transport.md) | Issuing AT commands — `atcli_smd11`, `qcmd`, `flock` serialization, `sms_tool`, and the hard URC boundary (no resident AT listener, `smd11` is not a selectable `AT+QURCCFG="urcport"` destination, the two parsers a stray unsolicited line would break, and the polled alternative) |
| [cellular-basic-settings.md](cellular-basic-settings.md) | Cellular Basic Settings (`/cellular/settings`) — the six writable fields and their AT commands, why `mode_pref` keeps all seven values a control must be able to represent, why `CFUN=0` is "Radio off" and never "Low power", the ~35s SIM-slot apply with its read-back verification and second radio cycle on failure, why the client recovery window keys on *attempted* rather than applied fields, the render-phase dirty-state merge rule (a refresh may only update untouched fields), `AT+QSIMDET` hot-swap detection with its UNVERIFIED reboot persistence and the `/etc/qmanager/` seed fallback, the poller's `+QSIMSTAT?` ordering rule and new optional `.sim` block, `network.type` now being legitimately `""`, and the `shapes.ts` geometry/tone contract |
| [cellular-settings-family.md](cellular-settings-family.md) | Any of the four other `/cellular/settings/` routes (APN Management, Network Priority, IMEI Settings, Blocked Networks) or the `shapes.ts` contract they now share — the dirty-row promotes-to-`primary-container` tone rule and its `_ON_FILL` twins, why the `Input` primitive is unusable here (tailwind-merge keeps `dark:`/`md:`-scoped classes against unprefixed overrides) and the `FIELD_SHELL` pair that replaces it, `RAT_RANK_TONE`'s neutral-for-WCDMA decision replacing a `RAT_COLORS` map that spent `success`/`destructive` on radio identity, the `qm_imei_reboot_pending` sessionStorage contract behind the now-real deferred reboot, Luhn gating both IMEI write paths, the FPLMN `unknown` state that stops a failed read asserting "no fault", the `detect_active_cid()` confidence gap that constrains the APN CID chip's wording, and the discarded `raw_data` follow-up |
| [qmanager-independence.md](qmanager-independence.md) | Installer, Entware bootstrap, `/dev/smd11` udev permissions, CGI auth, service persistence, firewall, Tailscale, web console, email/SMS alerts, OTA pipeline |
| [tmp-file-ownership.md](tmp-file-ownership.md) | Any `/tmp` file written by both root daemons and `www-data` CGI — the sticky-bit and `fs.protected_regular=1` rules (and which direction each actually blocks), the `root:root 0666` seeding contract in `qmanager_setup`, the never-`mv`/never-`rm` in-place write rule, and the `/tmp/qmanager_recovery_active` claim-by-read-back protocol with its 120s staleness ceiling |
| [alerts.md](alerts.md) | Centralized Alerts (`/monitoring/alerts`, `monitoring/alerts.sh`, `alert_engine.sh` — SMS/email/Discord routing×capability matrix, boot-id reboot ledger, Discord IPC contract, split-ownership boundaries, and the `/etc/qmanager-secrets/` credential store + its two root helpers) |
| [discord-bot.md](discord-bot.md) | The Discord bot (`discord-bot/`, `qmanager_discord` — now a pure DM transport driven by `alert_engine.sh`) |
| [band-locking.md](band-locking.md) | Band Locking (`/cellular/cell-locking`) — the two-axis band chip (fill carries SELECTED, a 2px inset ring carries LIVE, and why the ring is a shadow not a border), `unlockAll` writing the full supported list rather than clearing, the `prevLockedKey` render-phase sync that is the only thing repainting the grid after a write, the time-dependent `resolveScheduledScenario` gate chain, why `isBusy` blocks all three categories (`lock.sh` kills the previous failover watcher), per-category error scoping, the `categoryShortKey` fix for the string-surgery toast, the shared `CellularPageHeader`, the untouched onboarding implementation — plus the 2026-08-22 pass: the neutral carrier tile (identity on a 40px disc, quality on the ramp's numeral + `MetricBar`) and the three workarounds the old body tint had forced, failover moving from the rail's foot to a hero-spanning row (a reversal of a documented decision), and the Refresh action with its `isLoading`/`isRefreshing` split, its `failover.watcher_running` guard and the foreign-watcher adoption effect |
| [tower-locking.md](tower-locking.md) | Tower Locking (`/cellular/cell-locking/tower-locking`) — the two clocks (live `carrier_components` vs. the once-on-mount `status.sh` read-back and why polling it costs three AT commands on the shared mutex), the **unbounded** `qmanager_tower_failover` watcher and the permanently-dead lock switches its `watcher_running` guard caused, failover releasing BOTH radios, unlock silently clearing `.failover.enabled`, the one-write/two-read-backs `save_ctrl` `split` chip, the `service_enable_failed` / `persist_command_failed` warning codes, the on-air tile as picker under the Identity-Never-Acts Rule, the `LEG_BADGE` locked=warning inversion, NR-SA SCS provenance, and the one-directional frequency-lock gate |
| [frequency-locking.md](frequency-locking.md) | Frequency Locking (`/cellular/cell-locking/frequency-locking`) — the allow-list model (channels only, no PCI), the real AT parameter `lte_earfcn_lock` / `nr5g_earfcn_lock` with its COLON-separated values and count field, why there is **no persistence key** and reboot survival is unverified, the one-directional tower-lock gate (QManager's CGI refuses; `tower/lock.sh` has no reciprocal guard so a tower lock silently wipes a frequency lock), the **absent failover watcher** and the crash-dump risk it leaves unrecovered, why the apply bar renders the modem's read-back and never the AT command, channel frequency vs band centre, the serving-cell-only SCS, and the `LOCK_BADGE` locked=warning inversion |
| [cell-scanner.md](cell-scanner.md) | The Cell Scanner family (`/cellular/cell-scanner/**`) — the ~100x cost asymmetry between `AT+QSCAN=3,1` (30–180s on the global AT mutex) and `AT+QENG="neighbourcell"` (~2s) and why `COST` is a required hero slot, why the two scanning routes are deliberately NOT merged while sharing eleven primitives, why the frequency calculator takes the family's anchor geometry (`CALC_HERO` *is* `RUN_HERO`) and none of its run vocabulary, the `ulEarfcnOffset` fix that retired a hardcoded `+18000` uplink EARFCN (wrong on B30/B66/B71), band-agreement verdicts for overlapping NR ranges, and the `earfcnHistory` localStorage migration, the `/tmp/qmanager_long_running` maintenance contract (written by `qmanager_cell_scanner` as www-data, read by the root poller and watchdog, expired at 300s, and why `fs.protected_regular` makes the reverse ownership unsafe), the shared `/tmp/qmanager_scan.lock` singleton (one `flock` inherited by the spawned worker, why that makes it self-healing, why the lock file must never be unlinked, and why `other_scan_running` is a distinct error code from `already_running`), the non-destructive status endpoints and the result-before-error ordering they forced, the deliberately divergent 3-tier -85/-100 thresholds vs `RSRP_THRESHOLDS`, the 0-dBm sentinel, LTE-only locking on the neighbour route, the `CellScanResult` type inversion that was fixed, and why `NeighbourCellResult` stays a separate type |
| [carrier-aggregation.md](carrier-aggregation.md) | Carrier aggregation (`AT+QCAINFO` parsing in `parse_ca_info()`, the resource-block vs QENG-enum bandwidth split, `earfcn` carrying NR-ARFCN, the NSA one-PCC rule, empty-array-on-AT-failure, the `lib/carrier-aggregation.ts` view model and the dashboard CA strip) |
| [radio-information.md](radio-information.md) | The `/cellular/` index page, Cellular and Radio Information (`resolveRadioMode`'s load-bearing branch order and why non-registered modes replace the body, why `isLoading` never branches, counts from grouping `carrier_components[].technology` and never `ca_count`/`nr_ca_count`, the three distinct meanings of "no value", the serving-cell-only SCS and its `scsInferred` marker, `carrierKey` over index or PCI, the stale freeze, the wall-clock `Date.now()`, the three-facts-three-channels colour split, the accordion `height` Transform-Only exception, the deliberately narrow Copy diagnostics payload, and what was cut from the mock) |
| [recent-activities.md](recent-activities.md) | The dashboard event feed (`events.sh` NDJSON → `fetch_events.sh` → `use-recent-activities`, the `lib/event-presentation.ts` unresolved-condition pairing, glyph-carries-severity + `sr-only` word, error-vs-empty states, the two-animation motion budget, the message-bearing React key, `activities.*` i18n) |
| [dashboard-chart-cards.md](dashboard-chart-cards.md) | The dashboard chart cards (Device Metrics / Live Latency / Signal History) — the recipe-16 `.chart-draw` CSS-over-recharts motion contract, the two-clock split in `hooks/use-chart-motion.ts` (transient entrance class vs. recharts-owned poll morph) and `pathLength={1}`, the silent-failure contracts (`accessibilityLayer`, `useId` gradient ids, `connectNulls`, relative-domain strings, `chartConfig` keys), Signal History's single `CHART_H`, the reachability-toned latency chip, `MetricBar`'s `baseTone` vs `colorOverride` and its static-class-map lesson |
| [dashboard-state-motion.md](dashboard-state-motion.md) | Dashboard state-change motion — the live value tick cascade (`TickGroup` renders no DOM, ranks among CHANGED values not ordinal position, sorts live nodes via `compareDocumentPosition`, drains on a microtask, 40ms row step clamped at `MAX_RANK` 7) plus the `fill: "backwards"` retarget contract it forced in `use-value-tick.ts`, and the status chip morph's label half (`SwapLabel`: both crossfade legs, glyph inside, `sr-only` name outside, key encodes text AND variant). Also **Part 3 — the product-wide save flow**: `SaveButton`'s grid-stack width lock (why `AnimatePresence` is forbidden), the `SAVE_CHECK_OVERSHOOT` 1.03 constant that replaced an unbounded spring, the no-`aria-live`/no-`disabled` accessibility calls, and the three cards whose data-derived `key` remounts the "Saved!" flash away |
| [auth-rate-limiting.md](auth-rate-limiting.md) | The login lockout ladder (`cgi_auth.sh` 30/120/300/900s progressive lockout, `/tmp/qmanager_auth_attempts.json` schema + lock file, the read-only `qm_get_rate_limit_status` invariant that keeps `check.sh` from extending a lockout on every splash load, the subshell/re-read invariant, global-not-per-IP and tmpfs-state tradeoffs, the `attempts_remaining: 0`-while-unlocked gotcha, `scripts/test/auth-lockout-ladder.sh`) |
| [icon-system.md](icon-system.md) | Icons — the Icon-Boundary Rule (Material Symbols on the sidebar + dashboard route + the pre-auth `/` and `/login/` routes, lucide elsewhere; route-scoped not directory-scoped, and `/setup/` is deliberately excluded), the self-hosted subset pipeline (`bun run icons:subset`, `FILL` must stay a range), the single-source glyph list plus the `bun run icons:check` manifest gate that catches a stale font, the inline-`fontSize` sizing gotcha, the two Network Status exceptions, and the dashboard's service-ring / identity-chip / 13px decisions |
| [color-system.md](color-system.md) | Any colour token, or a surface that "reads loud" — the role families and their five-token shape, the Direction-Is-Not-A-Radio rule (Downlink Rose / Uplink Cyan on their own axis, direction never painted with a radio identity hue), the 40-degree hue-slot arithmetic that leaves hue 341 as the last free window and the palette full, the 2026-08-16 dark identity-fill tone-down (and why L 0.76 was skipped), the light `--lte-container` protanopia correctness fix, the **never equalise `--primary-container` and `--lte-container`** trap, and the measured CVD collapse floor that makes a dark container tint decoration rather than information |
| [antenna-alignment.md](antenna-alignment.md) | The antenna alignment tool (`/cellular/antenna-alignment`) — the port-major twin, the Live Aim instrument card and why it declines the value tick, the shared sentinel boundary it delegates to, the display-scale vs. ranking-scale split, `scoreSnapshot` as a pure function of the snapshot (no live `RadioMode`), missing legs renormalizing instead of scoring zero, the modem-timestamp sampling gate behind the honest ~8s 3-sample average, why `isDefaultLabel` keeps the storage key at `v1`, and the unreachable-vs-no-readings condition split on the shared `condition-screen.tsx` primitive |
| [antenna-statistics.md](antenna-statistics.md) | Antenna Statistics (`/cellular/antenna-statistics`) — the per-metric `SIGNAL_SENTINELS` boundary in `types/modem-status.ts` and the 117-sample capture behind it, why idle port blocks stay mounted rather than filtered or dimmed, the glyph-carries-quality verdict chips, `worstSignalQuality` per port, the SNR-vs-SINR label reuse of `radio_info.bands.metric.*`, and the unreachable-vs-no-signal state split |
| [custom-dns.md](custom-dns.md) | The Custom DNS feature (`/local-network/custom-dns`, dnsmasq upstream override via sentinel block in `/etc/data/dnsmasq.conf`) |
| [data-usage-counter.md](data-usage-counter.md) | The persistent data-usage counter (kernel `/proc/net/dev`-sourced, schema v5 with static SoC-based orientation) |
| [dpi.md](dpi.md) | Traffic Engine / DPI bypass (`/local-network/traffic-engine`) — the zapret `tpws` recipe and why `--tlsrec` was dropped, the two mutually exclusive modes and the inert `sni_domain` key, manifest+pin binary provisioning, the fast.com verify helper, the privilege-free status probe, and the **uninstall teardown contract**: nothing but `dpi_state.sh` owns the `nat` PREROUTING REDIRECT, `qmanager_dpi_run --clear` is authoritative, and skipping it is a LAN web outage rather than a leak |
| [ethernet.md](ethernet.md) | Ethernet Status & Link Speed (`/local-network/ethernet`, RTL8125B on `eth0`, sysfs link state + `ethtool` speed/duplex, `qmanager_ethernet_apply` root helper, and why an idle device's `qmanager-ethernet.service` correctly reads `inactive` rather than `failed`) |
| [wan-profile-management.md](wan-profile-management.md) | WAN Profile / APN management (`cellular/apn.sh`, 6 PDP contexts AT-only backend, pixel-strict single-APN page UI, `apn_setting.json` sidecar) |
| [sim-profiles.md](sim-profiles.md) | Custom SIM Profiles **and Connection Scenarios — one merged page** at `/cellular/custom-profiles` (the "in force now" hero + 24-hour schedule ribbon, the wizard-in-a-dialog, the retired `connection-scenarios` redirect route, 4-step apply, `scenario`/`scenario_id` binding + schedule windows via systemd timer, ICCID canonicalization, `--auto` apply supersession, gate matrix, `profile_managed` guard, **no band fields in the profile schema** — bands only via a bound custom scenario, band-failover watcher on the apply path, `current_settings.sh` + `+QSPN` carrier identity, create-form autofill-on-mount, carrier-matched suggested profiles) |
| [scheduled-timers.md](scheduled-timers.md) | Scheduled Reboot + Tower Lock Schedule (runtime-armed systemd `OnCalendar` timers replacing dead crond; shared `schedule_timer.sh`, `qmanager_scheduled_reboot_arm` / `qmanager_tower_schedule_arm`, `armed` flag to UI, OTA re-arm, the "crond binary exists but never runs" gotcha) |
| [sim-detection.md](sim-detection.md) | SIM detection — the known-SIMs set model (`sim_db.sh`, frozen `known_iccids` format), the persistent SIM registry sidecar (`sim_registry.json`, root helper, CGI, poller-derived `sim_swap.detected`, OTA migration, Tracked SIMs UI), byte-parity vs. canonicalized ICCID comparison, the watchdog/slot-switch/profile-activate `sim_db_add` coupling |
| [connection-watchdog.md](connection-watchdog.md) | Connection Watchdog (`qmanager_watchcat` 4-tier recovery ladder, Tier-3 SIM failover / Golden Rule / 90s settle floor / `verify_quimslot` read-back gate, misconfig-stops-ladder, two-pass CGI validation, status-first UI) |
| [connection-quality.md](connection-quality.md) | Connection Quality — the measurement/telemetry side (`qmanager_ping` shell ICMP producer with IPv4-first / IPv6-fallback, poller latency/jitter/loss stats, Probe Targets + Latency & Loss Thresholds cards, `ping_profile.json` two-writer contract) |
| [overview-splash.md](overview-splash.md) | The pre-auth surfaces `/` and `/login/` (unauthenticated landing, the 3-state `/` gate, 3 public CGI endpoints under `public/`, `credentials:"omit"` hooks, logout→`/` wiring; plus the Material-3 retarget — the `components/public/overview/` split, `resolveBodyMode()`, the `TonalBanner` primitive, warning-not-destructive tonal states, band identity chips, the motion recipes, and the pre-auth type scale) |
| [timezone.md](timezone.md) | Timezone / system clock (glibc `/etc/localtime` via `qmanager_timezone_apply` root helper, zoneinfo at `/opt/share/zoneinfo`, `sys_get_effective_tz` ground-truth, cron adopts zone on next reboot) |
| [sms.md](sms.md) | SMS Center (`/cellular/sms`, `sms.sh`, patched `sms_tool`, CPMS ME+SM dual-storage routing, boot routing oneshot, client-side read/unread) |
| [speedtest.md](speedtest.md) | Speed Test (Ookla CLI, `at_cmd/speedtest_*.sh`) — the ~400 MB-per-run data cost, the `flock`-serialized destructive harvest in `speedtest_status.sh` and its two ordering rules (`rm PID_FILE` last, `install -d` before the idempotence guard), the OTA hoist out of `install_dependencies()`, the HTTP-200 failure convention on the server list, the two-consumer `setTimeout` poll loop, the `PING_FLOOR_MS` hold, `lib/speedtest-phases.ts`, and the one-hue-per-measurement colour contract |
| [sms-forwarding.md](sms-forwarding.md) | SMS Forwarding (`/cellular/sms/forwarding`, `qmanager_sms_forward` daemon, seed-on-first-run, loop guard, retry/failures, gated service) |
| [platform-matrix.md](platform-matrix.md) | Any fact that differs **by device** — the canonical per-device table (RM520N-GL vs RG501Q-EU): BusyBox versions, the `wget`-absent Entware bootstrap trap and its curl shim, vendor-vs-Entware `lighttpd`, `PATH` ordering (`/opt/bin` before `/usr/bin` in the vendor default, absent entirely inside systemd units), rootfs/`ubi2_0` volume split (including `/opt` as a bind of `/usrdata/opt` on **both** devices — there is no `/opt`-shaped divergence), `/tmp` semantics, AT transport, CPU/ABI, the SSH addresses and identity-proof rule for both devices, and the numbered defect register (F6, F8–F14 — including the Entware `S80lighttpd` port-80 defect, now measured and reboot-validated on the RM520N-GL too, the world-writable `0666` Entware unit files measured on both devices, and the `rc.unslung` boot-ordering follow-up) |
| [platform-profile.md](platform-profile.md) | The advisory hardware profile `/etc/qmanager/platform.json` — `hw_profile.sh`'s parser/tier table/generator, the boot-time self-heal in `qmanager_setup` and its three regeneration triggers (including why a **higher** on-disk schema regenerates too), the symlink/directory refuse arms and the converge re-check that together stop an every-boot rewrite of `ubi2_0`, the unreadable-vendor-file deferral, the line-oriented parse contract, why logging goes to `/tmp/qmanager.log` rather than a journal with no storage, and the measured `opt.mount` boot race that is the real reason this path may never use `jq` |
| [lan-gateway-ip.md](lan-gateway-ip.md) | LAN gateway IP — **planned, not built**. Feasibility notes from the live 2026-08-25 validation: `/etc/data/mobileap_cfg.xml`, why `StartIP`/`EndIP` must move with `APIPAddr`, the `GatewayURL` decoy, no `xmllint`/`xmlstarlet` on the RG501Q-EU, and the `AT+CFUN=1,1` apply |
| [rg501q-bringup.md](rg501q-bringup.md) | Bringing QManager up on the RG501Q-EU (SDX55) — the 2026-08-24 adb probe, the GitHub/GFW finding, the Entware chicken-and-egg (`opkg` shells out to a `wget` this device does not have) and its 2026-08-25 installer fix, the one-shot poison-pill bootstrap guard, and why OTA cannot deliver either fix |
| [gui-installer.md](gui-installer.md) | The Windows GUI installer sub-project (`installer-gui/`) — the offline install path for users behind the GFW: the `__QM_RC=` exit-code sentinel and `wrap_command`'s conditional `&` separator, the `2>&1`-binds-to-`echo` trap, why SSH deliberately ignores paramiko's channel exit status (post-wrapping it is unconditionally 0, and the guard that checked it crashed preflight on every fresh device), the always-`--no-reboot` split and why a dead transport during reboot is the success path, the deliberate absence of a cancel gate after the script exits 0, the fail-closed model gate (`--force` disables `install_rm520n.sh`'s own RM551E `die`, not just its tty prompt) and the fail-closed Entware gate, the `; true` and `df -k` probe details, `tokens.css` extraction needing `@theme inline` **plus** `:root` **plus** `.dark`, `stage_payload` catching VERSION drift but not content drift, and the two defects that exist only in the frozen PyInstaller build |

For broader architecture, see `../rm520n-gl-architecture.md` and `../ARCHITECTURE.md`.
